document.addEventListener('DOMContentLoaded', () => {
    console.log('Javascript Loaded');

    // initialize modules
    ThemeManager.init();
    SmoothScroll.init();
    TypewriterEffect.init();
    FadeEffect.init();
});